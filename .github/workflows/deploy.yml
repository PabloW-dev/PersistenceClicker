# deploy automático para más tarde para producción, habrá que quitar la raíz de public para dejar de usar vite cuando toque o crashea todo

#name: Deploy to Github Pages

#on:
#  push:
#    branches:
#      - main # se ejecutará al hacer push a main
#jobs:
#  build-deploy:
#    runs-on: ubuntu-latest

#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v3
      
#      - name: Setup Node.js
#        uses: actions/setup-node@v3
#        with:
#          node-version: 22
      
#      - name: Install dependencies
#        run: npm install
      
#      - name: Build project
#        run: npm run build
      
#      - name: Deploy to Github Pages
#        uses: peaceiris/actions-gh-pages@v6
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: ./dist